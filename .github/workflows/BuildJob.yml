name: C/C++ CI

on: [push]

jobs:
  build:
    name: Build
    runs-on: [ubuntu-latest, windows-latest]
    
    steps:
    - uses: actions/checkout@v1
    - name: configure
      run: ./configure
    - name: make
      run: make
#    - name: make check
#      run: make check
#    - name: make distcheck
#      run: make distcheck
